import NavPageLayout from "../components/NavPageLayout";

export default function SignupPage() {
  return (
    <NavPageLayout title="Sign Up / Login">
      <p>Create an account to access the full Proof of Human platform.</p>
    </NavPageLayout>
  );
}
