# natte Chrome extension

Download [`natte-chrome.zip`](./natte-chrome.zip), unzip it, then load the folder in Chrome via `chrome://extensions` → **Load unpacked**.

Full setup: see [`../../extension/README.md`](../../extension/README.md).

Regenerate this zip from the repo root:

```bash
npm run extension:pack
```
